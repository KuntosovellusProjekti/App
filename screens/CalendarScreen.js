import { SafeAreaView} from "react-native";
import Calendars from "../components/Dashboard/Calendar";

export default function CalendarScreen () {

  return(

    <SafeAreaView>
      <Calendars/>
    </SafeAreaView>
  )
}